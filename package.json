{
  "name": "fimhub-vercel",
  "private": true,
  "version": "1.0.0",
  "type": "commonjs",
  "scripts": {
    "build": "npm --prefix frontend2.0/fimhub run build",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend2.0/fimhub run dev",
    "smoke": "npm --prefix backend run smoke"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "csv-parse": "^6.1.0",
    "csv-parser": "^3.2.0",
    "express": "^5.1.0",
    "fs-extra": "^11.3.2",
    "papaparse": "^5.5.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11"
  }
}
